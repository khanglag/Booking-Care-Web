steps:
  - name: 'gcr.io/cloud-builders/mvn'
    args: ['clean', 'package']
  - name: 'gcr.io/cloud-builders/gcloud'
    args: ['app', 'deploy', 'target/app-name-0.0.1-SNAPSHOT.jar']

timeout: 900s

options:
  default_logs_bucket_behavior: REGIONAL_USER_OWNED_BUCKET
